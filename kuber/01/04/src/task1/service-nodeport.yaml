apiVersion: v1
kind: Service
metadata:
    name: multi-container-nodeport
spec:
    selector:
        app: multi-container-app
    ports:
        - name: nginx-port
          protocol: TCP
          port: 80
          targetPort: 80
          nodePort: 30080
    type: NodePort